

        // i = 0;
        // b = i++;
        // console.log(b);
        // console.log(i);


        // a = 3;
        // c = ++a;
        // console.log(c);



for (let i = 0; i < 12; i++) {
    let isDivisible = i % 2 === 0;

    if (isDivisible) {
        alert(`${i} isDivisible by 2`)
    } else {   
        alert(`${i} is an odd number`)
    }
} 
