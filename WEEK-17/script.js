// Adding elements to an Array


// pop; this is used to remove the last element in an array...
// unshift; adds element to the beginning of the array
// push; thi is used to add element to the end of the array
// shift; this removes element at the beginning of an array


const example = [];
example.unshift(90);
console.log(example);
example.unshift(102);
console.log(example);
example.unshift(-1);
console.log(example);
example.unshift("Bala Blue");
console.log(example);



// splice
// splice is used to remove or replace elements in an array
